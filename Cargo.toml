[package]
name = "gcflobdd"
version = "0.1.0"
edition = "2024"

[dependencies]
lazy-regex = "3.6.0"
parking_lot = { version = "0.12.5", optional = true }

[features]
default = []
sync = ["dep:parking_lot"]
separate_reduce_map = []

[[test]]
name = "n_queens"
harness = false
